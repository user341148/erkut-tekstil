function changeVisionImage(imagePath) {
    const visionImage = document.getElementById('vision-image');  /*html belgesinde id değeri 'vision-image' olan öğeyi seçip visionImage değişkenine aktarır*/
    visionImage.src = imagePath;
}
